

export function getSteeringAndAcceleration(speed: number, leftDistance: number, frontDistance: number, rightDistance: number) {

    //TODO fuzzify

    //apply rules (first define rules)

    //defuzzify

}